from setuptools import setup

setup(name='squeaklib',
      version='1.0.0',
      packages=['squeaklib'],
      install_requires=[
            'photobot; python_version >= "2.7.0"',
            'Pillow'],
      )

